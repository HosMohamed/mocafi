export interface UserPayload {
  name: string;
  gender: string;
  email: string;
  status: string;
}

export interface UserResponse {
  id: number;
  name: string;
  email: string;
  gender: string;
  status: string;
}
